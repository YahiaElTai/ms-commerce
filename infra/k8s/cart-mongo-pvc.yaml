apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cart-mongo-pvc
  labels:
    app: cart-mongo
spec:
  storageClassName: ssd-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
