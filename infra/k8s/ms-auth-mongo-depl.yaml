apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-auth-mongo
  template:
    metadata:
      labels:
        app: ms-auth-mongo
    spec:
      containers:
        - name: ms-auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: ms-auth-mongo-srv
spec:
  selector:
    app: ms-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
